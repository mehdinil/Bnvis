
import 'package:flutter/material.dart';

class CoachView extends StatelessWidget {
  const CoachView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('کوچ AI'),
      ),
      body: const Center(
        child: Text('صفحه کوچ AI'),
      ),
    );
  }
}
